<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="stylesheet" href="styles.css" />
    <title>ConvertMyMoney</title>
  </head>
  <body>
    <%- include('header') %>
    <div class="result">
      <% if(error) { %> <%= error %> <% } else { %> <%= quantity %> of dollars
      on price <%= price %> is equals:
      <span class="real"><%= convertion %> reais</span>. <% } %>
    </div>
  </body>
</html>
